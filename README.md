Sources for the plugin `plugin.OneSignal`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.OneSignal" = {
            publisherId = "com.onesignal",
        },
    },
}
```
